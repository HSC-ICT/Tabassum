// create a calculator using if else statement

#include <stdio.h>

int main() {
    char operator;
    printf("Enter an operator (+, -, *, /, %%): ");
    scanf("%c", &operator);

    float n1, n2;
    printf("Enter two numbers: ");
    scanf("%f %f", &n1, &n2);


return 0;
}